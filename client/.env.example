# Only needed for production builds. In local dev, leave this unset —
# Vite's dev server proxies /api straight to the backend (see vite.config.js).
#
# Once your backend is deployed (e.g. on Render), set this to its URL with
# no trailing slash:
VITE_API_BASE_URL=https://shamba-log.onrender.com